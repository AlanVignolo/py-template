[project]
name = "py-template"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AlanVignolo", email = "alanvignolo1999@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "numpy<2",
    "pandas>=2.2",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.15.0",
]

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
